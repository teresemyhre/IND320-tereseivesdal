[theme]
primaryColor = "#6d86ff"  # A vibrant blue for primary elements
backgroundColor = "#1a1a2e" # Dark background for the main content
secondaryBackgroundColor = "#2e2e4e" # Slightly lighter dark for secondary elements (e.g., sidebar, input fields)
textColor = "#e0e0e0" # Light gray for general text
font = "sans serif" # Clean, modern font

[server]
runOnSave=true